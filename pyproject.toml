[project]
name = "circuitydatabase"
version = "9"
dependencies = [
    "typer",
]
requires-python = ">=3"
license = "MIT"
readme = "README.md"

[project.scripts]
circuity = "circuitydatabase.cli:app"
