[project]
name = "circuitydatabase"
version = "8"
dependencies = [
    "typer",
]
requires-python = ">=3"
license = "MIT"
readme = "README.md"

[project.scripts]
circuity = "circuitydatabase.cli:app"
